var globalVar = "Payments"; //This is the main subject (global variable)

console.log(globalVar);

const price1 = 35000; //Tesla's Price (block)
const price2 = 12000; //Money that you want to put in already (block)
let total = price1 - price2; //The math section (block)
document.getElementById("demo").innerHTML =
"The total is: " + total; //String variable